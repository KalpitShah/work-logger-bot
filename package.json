{
  "name": "slack-work-logger",
  "version": "1.0.0",
  "description": "Daily work logger Slack bot that DMs users, parses their replies via Socket Mode, and logs hours to a Google Sheet.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT",
  "dependencies": {
    "@slack/bolt": "^4.7.3",
    "@slack/socket-mode": "^2.0.4",
    "dotenv": "^16.4.5",
    "googleapis": "^144.0.0",
    "node-cron": "^4.2.1"
  }
}
